import * as XLSX from 'xlsx';
export const GPOS_FIELDS={dateTime:['วันที่-เวลา (Date-Time)','Date-Time'],invoiceNo:['เลขที่บิล (Invoice No.)','Invoice No.'],category:['หมวดหมู่ (Category)','Category'],itemName:['ชื่อสินค้า (Item Name)','Item Name'],qty:['จำนวน (Qty)','Qty'],unitPrice:['ราคาต่อหน่วย (Unit Price)','Unit Price'],discount:['ส่วนลด (Discount)','Discount'],total:['ยอดรวมสุทธิ (Total)','Total'],payment:['ช่องทางการชำระเงิน (Payment)','Payment']} as const;
export type GposRow={dateTime:string;invoiceNo:string;category:string;itemName:string;qty:number;unitPrice:number;discount:number;total:number;payment:string;errors:string[]};
const pick=(row:Record<string,unknown>,aliases:readonly string[])=>aliases.map(a=>row[a]).find(v=>v!==undefined&&v!==null);const text=(v:unknown)=>String(v??'').trim();const num=(v:unknown)=>Number(v??0);
export async function hashSourceRow(r:GposRow){const normalized=['GPOS',r.invoiceNo,r.dateTime,r.itemName.trim().toLocaleLowerCase('th-TH'),r.qty,r.unitPrice,r.discount,r.total].join('|');const buf=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(normalized));return [...new Uint8Array(buf)].map(b=>b.toString(16).padStart(2,'0')).join('');}
export async function parseGposFile(file:File){const data=await file.arrayBuffer();const wb=XLSX.read(data,{type:'array',cellDates:true});const ws=wb.Sheets[wb.SheetNames[0]??''];if(!ws)throw new Error('Workbook has no worksheet.');const raw=XLSX.utils.sheet_to_json<Record<string,unknown>>(ws,{defval:''});const headers=raw[0]?Object.keys(raw[0]):[];const missing=['dateTime','invoiceNo','itemName','qty','unitPrice','total'].filter(k=>!GPOS_FIELDS[k as keyof typeof GPOS_FIELDS].some(a=>headers.includes(a)));if(missing.length)throw new Error(`Missing required columns: ${missing.join(', ')}`);const rows:GposRow[]=raw.map(row=>{const r={dateTime:text(pick(row,GPOS_FIELDS.dateTime)),invoiceNo:text(pick(row,GPOS_FIELDS.invoiceNo)),category:text(pick(row,GPOS_FIELDS.category)),itemName:text(pick(row,GPOS_FIELDS.itemName)),qty:num(pick(row,GPOS_FIELDS.qty)),unitPrice:num(pick(row,GPOS_FIELDS.unitPrice)),discount:num(pick(row,GPOS_FIELDS.discount)),total:num(pick(row,GPOS_FIELDS.total)),payment:text(pick(row,GPOS_FIELDS.payment)),errors:[]} satisfies GposRow;if(!r.dateTime)r.errors.push('Missing date/time');if(!r.invoiceNo)r.errors.push('Missing invoice number');if(!r.itemName)r.errors.push('Missing item name');if(!(r.qty>0))r.errors.push('Qty must be greater than zero');if(r.unitPrice<0)r.errors.push('Unit price cannot be negative');if(r.discount<0)r.errors.push('Discount cannot be negative');if(r.total<0)r.errors.push('Total cannot be negative');const expected=r.qty*r.unitPrice-r.discount;if(Math.abs(expected-r.total)>0.05)r.errors.push(`Total mismatch (expected ${expected.toFixed(2)})`);return r;});const hashes=await Promise.all(rows.map(hashSourceRow));return {rows,hashes,summary:{sourceRows:rows.length,uniqueInvoices:new Set(rows.map(r=>r.invoiceNo)).size,quantitySold:rows.reduce((a,r)=>a+r.qty,0),grossSales:rows.reduce((a,r)=>a+r.qty*r.unitPrice,0),discounts:rows.reduce((a,r)=>a+r.discount,0),netSales:rows.reduce((a,r)=>a+r.total,0),invalidRows:rows.filter(r=>r.errors.length>0).length}};}